import SectionContainer from "../section/SectionContainer"

type Props = {}

const Footer = (props: Props) => {
    return (
        <SectionContainer>
            Footer
        </SectionContainer>
    )
}

export default Footer